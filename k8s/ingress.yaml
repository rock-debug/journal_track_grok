apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: journal-tracker-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: journal-tracker-service
            port:
              number: 80
